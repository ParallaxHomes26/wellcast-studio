[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
watchPatterns = [
  "artifacts/api-server/**",
  "lib/**",
  "Dockerfile",
  "railway.toml",
  "pnpm-workspace.yaml",
  "package.json",
  "pnpm-lock.yaml"
]
