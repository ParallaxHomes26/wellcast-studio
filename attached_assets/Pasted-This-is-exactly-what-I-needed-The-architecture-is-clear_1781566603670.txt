This is exactly what I needed. The architecture is clear now and there are two precise root causes:
Root Cause 1 — The generate endpoint can't find the profile

The backend uses supabaseAdmin with the service role key but it's reading VITE_SUPABASE_URL for the URL. That variable may not be available to the backend. Also the user_id being sent from the frontend may not match the actual Supabase UUID.
Root Cause 2 — Frontend Supabase client is using placeholder values

If VITE_SUPABASE_ANON_KEY isn't set correctly in Configurations, the client silently uses placeholder-anon-key and all auth calls fail quietly. This explains why the session isn't being detected properly.
Here's one surgical prompt that fixes both permanently. Paste this into Replit's agent:

Based on a full architecture audit I can see exactly what needs to be fixed. Please make these precise changes:
Fix 1 — Backend supabaseAdmin is reading wrong environment variable
In artifacts/api-server/src/lib/supabaseAdmin.ts the Supabase URL is likely reading VITE_SUPABASE_URL. Change it to read a backend-specific variable:
Show me the current contents of artifacts/api-server/src/lib/supabaseAdmin.ts first, then fix it to use:
typescriptimport { createClient } from '@supabase/supabase-js'

const supabaseUrl = process.env.VITE_SUPABASE_URL || process.env.SUPABASE_URL
const supabaseServiceKey = process.env.SUPABASE_SERVICE_ROLE_KEY

if (!supabaseUrl) throw new Error('SUPABASE_URL is not set')
if (!supabaseServiceKey) throw new Error('SUPABASE_SERVICE_ROLE_KEY is not set')

export const supabaseAdmin = createClient(supabaseUrl, supabaseServiceKey, {
  auth: {
    autoRefreshToken: false,
    persistSession: false
  }
})
Fix 2 — Generate route must handle missing profile gracefully
In artifacts/api-server/src/routes/generate.ts find where the profile is fetched and add this fallback:
typescriptconst { data: profile, error: profileError } = await supabaseAdmin
  .from('profiles')
  .select('*')
  .eq('id', user_id)
  .single()

console.log('Generate - user_id:', user_id)
console.log('Generate - profile:', profile)
console.log('Generate - profile error:', profileError)

// If profile doesn't exist create one on the fly
if (!profile) {
  console.log('No profile found - creating one')
  const now = new Date()
  const trialEnd = new Date()
  trialEnd.setDate(trialEnd.getDate() + 7)
  
  const { data: newProfile } = await supabaseAdmin
    .from('profiles')
    .upsert({
      id: user_id,
      subscription_status: 'trialing',
      subscription_tier: 'free_trial',
      trial_starts_at: now.toISOString(),
      trial_ends_at: trialEnd.toISOString(),
      run_count_this_month: 0,
      run_count_reset_at: now.toISOString()
    }, { onConflict: 'id' })
    .select()
    .single()
    
  // Use new profile or allow anyway
  const effectiveProfile = newProfile || { id: user_id, subscription_status: 'trialing' }
  const { allowed, reason } = canRunGeneration(effectiveProfile)
  if (!allowed) return res.status(403).json({ error: reason })
} else {
  const { allowed, reason } = canRunGeneration(profile)
  if (!allowed) return res.status(403).json({ error: reason })
}
Fix 3 — Frontend must send correct user_id to generate endpoint
In artifacts/wellcast-studio/src/pages/NewRunPage.tsx find where the generate API call is made and make sure user_id comes from the Supabase session:
typescript// Get fresh session before generating
const { data: { session } } = await supabase.auth.getSession()
console.log('NewRunPage - session:', session?.user?.id)

if (!session?.user?.id) {
  setError('Session expired. Please sign in again.')
  return
}

const generateResponse = await fetch('/api/generate', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    episode_brief: brief,
    cta: ctaValue || '',
    user_id: session.user.id,  // Must come from session
    input_method: activeTab
  })
})

console.log('Generate response status:', generateResponse.status)
Fix 4 — Verify VITE_SUPABASE_ANON_KEY is in Configurations
Check artifacts/wellcast-studio/src/lib/supabase.ts — if it's using placeholder values it means VITE_SUPABASE_ANON_KEY is not in Replit Configurations. Tell me if the console.warn for missing VITE_SUPABASE_ANON_KEY is firing.
Fix 5 — Add SUPABASE_URL as a backend Secret
The backend needs its own copy of the Supabase URL as a plain Secret (not a Vite Configuration). Add SUPABASE_URL to Replit Secrets with the same value as VITE_SUPABASE_URL.
List every file changed and show me the console.log output after the next generation attempt.